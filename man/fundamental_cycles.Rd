% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spanning_tree.R
\name{fundamental_cycles}
\alias{fundamental_cycles}
\title{Fundamental cycles of a spanning tree}
\usage{
fundamental_cycles(graph, tree)
}
\arguments{
\item{graph}{An \code{adj} object representing a connected undirected graph.}

\item{tree}{An \code{adj} object representing a spanning tree of \code{graph}.}
}
\value{
A list of \code{adj} objects, one per non-tree edge. Each \code{adj}
represents the cycle as a subgraph of \code{graph}.
}
\description{
Returns the fundamental cycles of a spanning tree with respect to the
original graph. There is one fundamental cycle per non-tree edge
(m - n + 1 total), formed by adding that edge to the unique path between
its endpoints in the spanning tree.
}
\examples{
g <- adj::adj(list(c(2L, 3L), c(1L, 3L), c(1L, 2L)))
t <- minimum_spanning_tree(g)
fundamental_cycles(g, t)
}
\seealso{
\code{\link[=fundamental_cuts]{fundamental_cuts()}}
}

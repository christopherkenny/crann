% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minimum.R
\name{minimum_spanning_tree}
\alias{minimum_spanning_tree}
\title{Minimum spanning tree}
\usage{
minimum_spanning_tree(graph, weights = NULL)
}
\arguments{
\item{graph}{An \code{adj} object representing a connected undirected graph.}

\item{weights}{A numeric vector of edge weights, one per edge. Edges are
ordered by iterating vertices \verb{u = 1, ..., n} and for each \code{u} iterating
over \code{graph[[u]]} retaining only neighbors \code{v > u}. Pass \code{NULL} for
uniform weights.}
}
\value{
An \code{adj} object representing the minimum spanning tree.
}
\description{
Finds a minimum spanning tree of a connected undirected graph using
Kruskal's algorithm. When \code{weights} is \code{NULL} (the default), all edges are
treated as having equal weight and any spanning tree is returned.
}
\examples{
g <- adj::adj(list(c(2L, 3L), c(1L, 3L), c(1L, 2L)))
minimum_spanning_tree(g)
}
\seealso{
\code{\link[=is_spanning_tree]{is_spanning_tree()}} to validate a spanning tree.
}
